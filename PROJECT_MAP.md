Last verified: 2026-09-04 @ b4a50ea

# PROJECT_MAP.md

Compressed navigation index for FinTrack. **This is an orientation tool, not a substitute for source code** — always read the actual file before modifying it; this map omits implementation detail by design. If a file contradicts this map, the source code is right — fix the map and flag it.

Codebase size at last verification: 182 Swift files, ~84,000 lines, across `FinTrack/` (iOS app), `FinTrackWidget/` (widget extension), `FinTrackWatch/` (watchOS companion).

Per-module detail lives in `docs/maps/` (this root file stays under 500 lines; the module maps do not):
- **`docs/maps/MAP_Models.md`** — every `@Model`/Codable struct, properties, relationships, cross-cutting data patterns
- **`docs/maps/MAP_Services.md`** — every service in `Core/Services/`, public method signatures, external APIs, error-handling patterns, actor-isolation quirks
- **`docs/maps/MAP_Features.md`** — every file in every `Features/` subfolder (one-line purpose each) + `App/`, `UI/`, `FinTrackWidget/`, `FinTrackWatch/`

## 1. OVERVIEW

FinTrack is a UAE-focused personal/family/business finance iOS app (SwiftUI + SwiftData, iOS 26 Liquid Glass design). Single Xcode project, no Package.swift, no CLI build, no automated test target — see root `CLAUDE.md` for build/run instructions and the design-system token reference. Architecture is a flat MVVM-ish SwiftUI style: `@Model` classes + `@Query` drive views directly, with ~38 singleton `Core/Services` classes doing all business logic/computation/networking (no separate ViewModel layer). Companion targets: a WidgetKit extension (5 widgets + a Live Activity) and a watchOS app, both reading a shared App Group `UserDefaults` snapshot rather than SwiftData directly.

## 2. DIRECTORY STRUCTURE

```
FinTrack/
  App/              Entry point, ModelContainer/Schema, AppState, AppTab, RootView    → MAP_Features.md
  Core/
    Models/         ~50 @Model classes + Codable value types                          → MAP_Models.md
    Services/       ~38 singleton services (business logic, networking, computation)  → MAP_Services.md
    Utilities/      Extensions.swift (Double/Date/View/Array helpers, see CLAUDE.md)
  Features/         24 subfolders, one per feature area (~140 view files)              → MAP_Features.md
  UI/
    Theme/          FTDesignSystem.swift, AppTheme.swift — design token source of truth
    Components/     AmountTextField.swift
FinTrackWidget/     WidgetBundle (5 widgets) + Live Activity UI, single file
FinTrackWatch/      watchOS companion app (3-tab TabView), reads shared App Group
docs/maps/          Per-module detail maps (see above)
```

## 3. DATA MODELS

~50 `@Model` classes, all registered in `Schema([...])` in `FinTrackApp.swift` (verified — no orphans). No `@Attribute(.unique)` anywhere (CloudKit compatibility constraint). Full catalogue with every property: **`docs/maps/MAP_Models.md`**.

Highlights worth knowing before touching the model layer:
- `Transaction` is the central hub model but has only 5 real SwiftData `@Relationship`s (`account`, `toAccount`, `linkedLoan`, `linkedBNPL`, `documents`); every other cross-model reference in the whole schema (Account matching, salary/lent/borrowed/debt links, tax/VAT links, etc.) is a loose `UUID?` foreign key, not a relationship. Follow this convention for new links.
- Embedded-array pattern is inconsistent: most models use the documented `Data + JSONEncoder/Decoder` computed-property pattern, but `Bill.priceHistory`, `BudgetTemplate.items`, and `Transaction.splitItems/recurringRule/tags` are native SwiftData arrays instead. Check the existing model before assuming which pattern applies.
- Every money-holding model carries its own `currency: String`. Summing/comparing these across records without `CurrencyService.convert(_:from:to:)` first is a real, recurring bug class (see STATE below).

## 4. CORE FEATURES

24 feature areas under `Features/`; full file-by-file breakdown in **`docs/maps/MAP_Features.md`**. Condensed map:

| Area | Primary files |
|---|---|
| Transactions (manual/receipt/voice/CSV) | `Transactions/AddTransactionView.swift`, `TransactionsListView.swift`, `CSVImportView.swift` |
| Email bank-alert ingestion | `Import/EmailImportView.swift`, `BankSetupWizardView.swift`, `EmailReviewQueueView.swift` + `EmailSyncService` |
| Apple Pay ingestion | `Import/ApplePayImportView.swift` + `ApplePayIngestService`/`ImportFiler`, same `EmailReviewQueueView` |
| SMS bank-alert ingestion | `Import/SMSImportView.swift` (Shortcuts setup + per-bank rules) + `BankSMSParser`/`SMSIngestService`, filed into the same `EmailReviewQueueView` |
| Budgets (monthly/annual/envelope/zero-based) | `Budget/BudgetView.swift` + `BudgetService` |
| Savings goals | `SavingsGoals/*` + `SavingsGoalService` |
| Debt (loans/cards/BNPL/lent/borrowed) | `Debt/DebtManagementView.swift`, `Accounts/AddAccountView.swift` (Add forms) + `DebtService` |
| Backup/restore (3 providers) | `Import/LocalBackupView.swift` (offline/on-device), `EmailBackupView.swift`, `GoogleDriveBackupView.swift` + `DataTransferService`/`BackupEncryptionService` |
| Net worth & assets | `NetWorth/NetWorthDashboardView.swift`, `Assets/*` + `NetWorthService` |
| Investments (stocks/crypto/gold) | `Investments/*` + `InvestmentService`, `CryptoPriceService`, `StockPriceService` |
| Income (salary/freelance/rental/dividends) | `Income/*` + `IncomeService` |
| Business/freelance (invoices, clients, mileage) | `Business/*` |
| UAE tax (VAT/FTA/Zakat/income tax) | `Tax/*` + `TaxService` |
| Family/household sharing | `Family/*` + `FamilyService` |
| AI analytics suite (on-device, no LLM) | `AIAssistant/*`, `Intelligence/*` + `AIAnalyticsService`/`FinancialIntelligenceService` |
| Premium advisory tools | `Premium/*` |
| Reports/PDF/CSV export | `Reports/ReportsView.swift` + `ReportExportService` |
| Security & settings | `Settings/*` + `BiometricService` |
| Siri/Widget/Watch quick-add | `AppIntents/FinTrackIntents.swift`, `FinTrackWidget/`, `FinTrackWatch/` + `WidgetDataService` |

## 5. KEY FLOWS

1. **Manual transaction entry**: `AddTransactionView` (+ optional `ReceiptScannerService`/`SpeechTransactionService` capture) → `AICategorizationService.predictCategory` → `Transaction` inserted → `Account.balance` adjusted → `WidgetDataService.updateAll` → `SpotlightService.indexTransactions`.

2. **Email bank-alert ingestion**: `EmailSyncService.runSyncPass`/`startAutoSync` (or `BGTaskScheduler` background) → IMAP fetch or Gmail/Outlook API → `BankEmailParser.parse` → `ImportLearningService` dedup/rename → `AICategorizationService` category guess → `PendingEmailTransaction` enqueued → `EmailReviewQueueView` (approve/reject, always a manual tap — see §8) → `EmailSyncService.approveToLedger` → real `Transaction` created, `Account.balance` adjusted, linked `BNPLPlan` advanced if applicable.

2b. **SMS bank-alert ingestion**: one Shortcuts "When I receive a message" Personal Automation (set up from `SMSImportView`, all bank senders selected under one "From") hands each raw text to the silent `LogTransactionFromText` AppIntent → `WidgetDataService.enqueuePendingSMS` (AppIntents don't reliably touch SwiftData, so nothing here does) → `RootView.drainPendingSMSTexts()` on next foreground → `SMSIngestService.ingest` → `BankSMSParser.parse` (on-device bank-template match, falling back to Apple's on-device `FoundationModels` only when no template matches, with evidence-grounding via `TextNormalizer.isGrounded`) → same `ImportLearningService` dedup/rename + `AICategorizationService` guess → a `PendingEmailTransaction` tagged `senderAddress = "sms:<bank>"` joins the **same** `EmailReviewQueueView`/`approveToLedger` pipeline as flow 2 above. No new `@Model`, no schema bump — see §8.

3. **Budget tracking**: `Transaction.spendingPairs` (base-currency, per category) aggregated by `BudgetView.spending(for:in:)` → compared against `Budget.amount`/`.rolloverAmount` (in the budget's own currency, converted via `CurrencyService`) → `BudgetService.checkAndSendAlerts`/`forecastEndOfMonth` → `NotificationService` fires threshold alerts; `BudgetLiveActivityService` can mirror progress to a Live Activity.

4. **Debt repayment** (Loan / BNPL / MoneyLent / MoneyBorrowed — all four follow the identical pattern): `RecordXPaymentSheet` creates a linked `Transaction` (`linkedLoan`/`linkedBNPL`/`linkedMoneyLentId`/`linkedMoneyBorrowedId`) → deducts the chosen `Account.balance` → updates the debt record's own progress fields (`outstandingBalance`/`paidInstallments`/`repayments` array). Deleting a single payment or the whole debt record **must** reverse the `Account.balance` delta first, or the deduction is orphaned — this exact bug was found and fixed for every one of the four debt types this session.

5. **Backup/restore** (3 provider paths converge on one substrate): `DataTransferService.exportBackup` (JSON of every `@Model`) → `BackupEncryptionService.encryptIfEnabled` (**mandatory** AES-GCM, random device-only Keychain key, never synced) → delivered via `LocalBackupService` (on-device `Application Support/Backups`, hidden from Files + a Keychain device snapshot; auto-runs after each edit) / `GoogleDriveBackupService` (OAuth+PKCE, `drive.file` scope) / `EmailBackupService` (SMTP to self, no OAuth). Restore reverses the pipeline: fetch → `BackupEncryptionService.decryptIfNeeded` → `DataTransferService.importBackup(mode: .merge/.replace)`. Every backup is ciphertext readable only by this FinTrack install (see §8).

6. **Siri/Watch quick-add**: `FinTrackIntents` (`LogExpenseIntent` etc.) or `WatchQuickExpenseView` → enqueue a pending-transaction into the shared App Group `UserDefaults` (key `pending_transactions`) → `RootView.drainPendingIntentQueue()` on app foreground/active → creates a real `Transaction`.

2c. **Apple Pay ingestion**: a Shortcuts **"Transaction"** Personal Automation fires on a Wallet payment and hands its *structured* fields (amount, merchant, date, category, card) to `LogApplePayTransaction` → `WidgetDataService.enqueuePendingApplePay` → `RootView.drainPendingApplePay()` on next foreground → `ApplePayIngestService.ingest` builds a `ParsedBankEmail` directly (no parsing — nothing can be misread) → `ImportFiler.file(channel: .applePay)` → same review queue as flows 2 and 2b, tagged `senderAddress = "applepay:<card>"`. More accurate than SMS where it applies, but only covers payments actually made through Apple Pay; running both channels is fine since cross-channel duplicates get flagged (§8).

## 6. SERVICES & PUBLIC INTERFACES

~38 singleton services under `Core/Services/`, almost all `final class X { static let shared }`. Full catalogue with method signatures, external APIs touched, and per-service actor-isolation notes: **`docs/maps/MAP_Services.md`**.

External network surfaces (all raw `URLSession` or hand-rolled sockets, no third-party networking SDK): `open.er-api.com` + 5 IRR fallback sources (FX rates), `api.binance.com`/CryptoCompare (crypto prices), `query1.finance.yahoo.com` (stock prices), Gmail REST / Microsoft Graph (OAuth email), Google Drive v3 REST (OAuth backup), `places.googleapis.com`/Nominatim (merchant category fallback), plus hand-rolled `IMAPClient`/`SMTPClient` over `Network.framework` for app-password email backup/import.

## 7. ERROR HANDLING & TESTING

**No automated test target exists in the Xcode project** — QA is manual/in-simulator only.

Error handling has three co-existing patterns (detail + file:line examples in `MAP_Services.md`):
1. Silent `try?` swallowing — dominant for persistence (`context.save()`) and notification scheduling.
2. Typed `LocalizedError` enums + `async throws`, caught at the service boundary and turned into a published `lastError: String?` for the UI — never re-thrown to SwiftUI. Used by backup/sync services (`BackupEncryptionError`, `EmailSyncError`, `DriveBackupError`, `EmailBackupError`).
3. Best-effort fallback chains instead of propagation, for external data fetching (FX rates, crypto/stock prices) — silently keep stale/cached data if every source fails.

## 8. CONVENTIONS & CONSTRAINTS

(Design-system tokens, spacing/radius/font rules, and the `Data`+`JSONEncoder` embedded-array convention are already documented in root `CLAUDE.md` — not repeated here.)

- **Local data encryption is mandatory and not user-configurable — no toggle, no algorithm name in any user-facing text.** `AppSettings.encryptionEnabled` was removed entirely (per explicit user request to make encryption "forced"); `SecurityPrivacyView`'s "End-to-End Encryption" row is a static, non-interactive `securityRow` reading "Always on", and `securityScore`/`scoreFeatureTile("Encrypted", ...)` hardcode it as always-active. Separately, "AES-256" wording was scrubbed from all user-facing strings that named the algorithm (`SecurityPrivacyView.infoCard`, `BackupEncryptionSettingsView`, `iCloudSyncView`) — replaced with generic "encrypted" phrasing (code comments naming AES-GCM are fine). **Backup encryption is likewise mandatory and non-configurable** (later change, superseding the earlier opt-in passphrase design): `BackupEncryptionService` now always encrypts with a random 256-bit key stored only in this device's Keychain (`kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`) — no user passphrase, no toggle, no "off" path. The old `storedPassphrase`/`isEnabled` API and the passphrase-management UI are gone; the backup-encryption **UI has been removed entirely** — there is no `BackupEncryptionSettingsView` and no Settings/iCloud row for it (backup encryption is silent/always-on, not surfaced to the user). Consequence to remember: a backup is decryptable **only by the same install on the same device** (the key never leaves it), so cross-device / post-reinstall restore is intentionally impossible — this is the "readable by nothing but this app" guarantee, and any future backup work must preserve it (never add a way to export/derive the key or turn encryption off). Any new encryption-related UI must follow the rules: no on/off control for core data or backup encryption, and never name the algorithm/mode to the user.

- **Schema versioning is wipe-and-recreate, not migration.** Bump `currentSchemaVersion` in `FinTrackApp.swift` (currently `"v28"` — CLAUDE.md's example of `"v15"` is stale) whenever adding a new `@Model` class or a **non-optional** property to an existing one; new **optional** properties (e.g. `Transaction.linkedBNPL` added this session) have not required a bump in practice. **Never bump without explicit user confirmation** — it wipes all local data on next launch.
- **Cross-model references default to a loose `UUID?`, not a `@Relationship`.** Only 6 true relationships exist in the entire schema (see §3). Follow this when linking a new model to an existing one.
- **Currency correctness**: any code that sums or compares two currency-tagged fields (`Budget.amount`, `SavingsGoal.targetAmount`, `Account.balance`, etc.) across records must call `CurrencyService.convert(_:from:to:)` first. Grep for raw `.reduce(0) { $0 + $1.amount }`-style sums on currency-tagged fields as a red flag — several real bugs of this shape were found and fixed in Budget/SavingsGoal/BudgetEnvelope totals across sessions (`BudgetView.swift`, `ReportsView.SavingsGoalsReport`, `AddSavingsGoalView`'s emergency-fund suggestion, `SmartCashAllocationView`'s goal-funding calc). `BudgetEnvelope` now has a working currency picker (`AddEnvelopeView`) — `EnvelopeRow` takes a pre-converted `allocatedAmount` param, `envelopeOverviewCard`/`EnvelopeDetailView` convert before combining with `spent` (always base-currency), and the envelope-to-envelope Transfer sheet converts into the destination's own currency. **Not yet fixed**: `SmartCashAllocationView.totalCash` (sums `Account.balance` across accounts, no conversion).
- **Debt repayment pattern** (Loan/BNPL/MoneyLent/MoneyBorrowed): recording a payment must create a linked `Transaction` AND deduct the paying `Account`'s balance; deleting that payment or the parent record must reverse the balance delta. Established across `DebtManagementView.swift`/`AccountDetailView.swift` after real "orphaned deduction" bugs were found and fixed for every one of the four types.
- **When deleting a whole MoneyLent/MoneyBorrowed record, reverse each linked `Transaction` by its own `.type`, never a single hardcoded direction.** Both `linkedMoneyLentId`/`linkedMoneyBorrowedId` now tag *two* kinds of linked transaction with opposite signs: the initial lend/borrow (created by `AddMoneyLentView`/`AddMoneyBorrowedView` — `.expense` for lending, `.income` for borrowing) and repayments (the reverse of each). `deleteLentItem`/`deleteBorrowedItem` (`DebtManagementView.swift`) and the duplicate `MoneyLentDetailSheet`/`MoneyBorrowedDetailSheet.deleteEntireRecord()` all originally reversed every linked transaction with one hardcoded `+=`/`-=`, correct only for repayments — fixed to `switch tx.type { case .expense: += ; case .income: -= }` per transaction. Per-repayment `deleteRepayment`/`updateRepayment` (scoped by `linkedDebtRepaymentId`, never matching the initial transaction) didn't have this bug and were left alone.
- **`.swipeActions` silently does nothing outside a `List`.** Several feature screens (`BudgetView.swift`'s tabs, `DebtManagementView.swift`'s tabs, `AccountsView.swift`'s tab list) use `ScrollView` + `VStack` instead of `List` for custom row styling, and `.swipeActions` on a row in that context never renders — no crash, no warning, just a dead no-op. Every row that needs swipe-style actions in a non-`List` container must use `.contextMenu` (long-press) instead; this is the established working pattern across Loans/BNPL/Lent/Borrowed/Budget/Goals/Envelopes/Accounts. When adding a new delete/edit/archive action to a row, check the enclosing container before reaching for `.swipeActions`.
- **A single fixed-width row wider than the content column stretches the ENTIRE form edge-to-edge** — this was the real root cause of the month-long "no margins" bug on the form sheets (savings goal, borrow/lent, etc.), finally proven on-device 2026-08-03. A `VStack` sizes to its widest child, so one `HStack` of N fixed-size color circles (e.g. 8×36pt + spacing ≈ 390–410pt > the ~353pt column on a 393pt phone) pushes the whole column wide; the `.padding(.horizontal)` then falls off-screen, section labels lose their leading glyph, and the pinned CTA's corners clip. **Any swatch/chip row with fixed-size items must wrap (`LazyVGrid(columns: [GridItem(.adaptive(minimum:…))])`) or live in a horizontal `ScrollView`** (AddAccountView's color rows were already correct this way). Fixed: `AddMoneyLentView`/`AddMoneyBorrowedView` `colorSwatches`, `AddSavingsGoalView` COLOR row, `BudgetView` AddEnvelope COLOR row. The form sheets additionally hard-pin their column via a root `GeometryReader` (`.frame(width: geo.size.width − 2×FTSpacing.screen)`), which contains the blast radius of any future over-wide child to its own section.
- **Never put `FTBackdrop()` (or any `.ignoresSafeArea()` view) as a sibling inside a `ZStack` that also wraps the screen's `ScrollView`, and never pin a CTA as a `ZStack(alignment: .bottom)` sibling.** `FTBackdrop` and the bottom save-button's gradient both call `.ignoresSafeArea()`; inside a `ZStack` that inflates the stack's bounds, and the `ScrollView` then lays out *wider than the screen* — its content's `.padding(.horizontal, FTSpacing.screen)` ends up off-screen, so section labels lose their leading glyph ("PERSON INFO" → "ERSON INFO"), cards run edge-to-edge, and the CTA's rounded corners get clipped. The CTA sibling also overlaps the last section. Correct pattern (per CLAUDE.md): `ScrollView { … }.background { FTBackdrop() }.safeAreaInset(edge: .bottom) { saveButtonArea }` — a `.background` never affects the parent's size and `safeAreaInset` reserves its own space. Swept app-wide: fixed in `AddMoneyBorrowedView`, `AddMoneyLentView`, `AddSavingsGoalView`, `AddInvestmentView`, `AddCryptoView`, `AddGoldHoldingView`, `AddBillView`, `AddSalaryRecordView`, `AddFreelanceProjectView`, `AddDividendView`, `AddRentalPropertyView`, `AddRealEstateView`, `VehicleListView`, and the nested `AddTenancySheet`/`RecordRentPaymentSheet` (RentalView) + `AddInvoiceSheet`/`RecordPaymentSheet` (FreelanceView) — 14 sheets in total; no instances of the pattern remain. Plain `ZStack { FTBackdrop(); … }` screens that don't pin a bottom CTA render fine and were left alone.
- **`.gridCellColumns(_:)` only works inside `Grid`, not `LazyVGrid`.** If a `LazyVGrid` needs one cell to span full width (e.g. a "wide" card in a 2-column grid), render it as a separate full-width view below/outside the `LazyVGrid` instead — the modifier is silently ignored in a `LazyVGrid` context (see `AccountsView.moduleGrid`).
- **`Button` wrapping a card styled with `.ftGlassInteractive`/`.ftGlass` (`glassEffect()`) needs an explicit `.contentShape(Rectangle())`.** Without it, taps only register on the card's actual content glyphs (icon/text) — not on `Spacer()` gaps or padding, which is most of a typical card. This produced real "tapping does nothing" bugs on the Accounts module grid and the Loan/BNPL/Lent/Borrowed cards in `DebtManagementView.swift`. Any new `Button { ... } label: { ...ftGlassInteractive... }` card needs `.contentShape(Rectangle())` (or a matching rounded shape) added.
- **Actor isolation**: Xcode project has Default Actor Isolation = MainActor, so plain classes/enums are implicitly MainActor-isolated. Mark pure-computation types `nonisolated` explicitly when they run via `Task.detached` (`BackupEncryptionService`, `KeychainStore`) or are short-lived non-UI network clients not meant to be actor-isolated at all (`IMAPClient`, `SMTPClient`, both `@unchecked Sendable`).
- **Tab bar is full** (4 tabs + centre FAB). New top-level modules must be reachable via `Settings/SettingsView.swift`, not a new tab (per CLAUDE.md).
- **FinTrackWidget has 5 widgets, not 3** — CLAUDE.md's "Platform Extensions" section is stale on this count (`FinTrackBalanceWidget`, `FinTrackBudgetWidget`, `FinTrackBillsWidget`, `FinTrackPaymentsWidget`, plus the Live Activity configuration).
- **Email account sign-in: OAuth (`ASWebAuthenticationSession`) for every provider that has one, password-IMAP only where OAuth is impossible.** `EmailProvider.supportsOAuthSync` is `true` only for `.gmail`/`.outlook` — both go through the single `EmailSyncService.connect(provider:context:)` OAuth+PKCE flow (`ASWebAuthenticationSession`); `EmailImportView.connect(_:)` no longer has a Gmail-specific password-IMAP fallback (removed — previously fell back to `IMAPSignInSheet` when no client ID was configured, now always opens `OAuthSetupSheet` like Outlook). `.icloud`/`.imap` stay on `IMAPSignInSheet` (app-specific password) because there is no public third-party OAuth authorization flow for iCloud Mail, and `.imap` is an arbitrary user-supplied host with no known OAuth endpoint — this is a protocol constraint, not a gap to close.
- **`GmailOAuthClientID` is baked into `FinTrack-Info.plist`** (real iOS-type OAuth client ID, safe to commit — Google treats native-app client IDs as non-confidential, PKCE covers the security). `EmailSyncService.storedClientId(for:)` checks this Info.plist key before the UserDefaults-backed manual-paste fallback, so Gmail sign-in requires zero setup for anyone building/running this repo: tap Gmail → straight to Google's real login page. No `CFBundleURLTypes` entry is needed for the reversed-client-ID redirect scheme — `ASWebAuthenticationSession` intercepts its own callback without app-level URL-scheme registration. Outlook has no equivalent baked-in client ID yet (still requires the one-time `OAuthSetupSheet` paste) since Microsoft Entra app registration wasn't done this session.
- **Before editing a form struct, grep for its call sites, not just trust that its file is the live one.** `AddMoneyLentView.swift`/`AddMoneyBorrowedView.swift` had a working account picker + linked-`Transaction`/balance logic added across two earlier sessions — invisible the whole time, because `DebtManagementView.swift`'s actual `.sheet(...)` presentations all called a separate, private, dead-simple `AddMoneyLentSheet`/`AddMoneyBorrowedSheet` (no account picker, free-text currency field, and — worse — no linked `Transaction`/balance deduction on the *initial* lend/borrow at all). Both files were only ever exercised by their own `#Preview` blocks. Fixed by repointing all 6 call sites in `DebtManagementView.swift` to the real `AddMoneyLentView`/`AddMoneyBorrowedView` and deleting the two dead private structs outright. **When a user reports a UI/behavior bug that contradicts source you just read, grep `StructName(` across the whole target (not just the definition site) before concluding it's a stale-build issue** — a same-named-but-different or a second, unwired struct is a real, recurring failure mode in this codebase.
- **`.tracking(1.6)` on an uppercase section-header `Text` clips its leading glyph unless paired with `.fixedSize(horizontal: true, vertical: false)`.** CLAUDE.md's own documented convention ("`.ftLabel` — section labels — pair with `.tracking(1.6)`") was copy-pasted into 68 files without this — every section header app-wide rendered with its first letter cut off ("PERSON INFO" → "ERSON INFO", "AMOUNT & DATES" → "MOUNT & DATES", etc.), confirmed identically on the Lend/Borrow Money screens and swept everywhere in one pass (229 occurrences). **Any new `.tracking(1.6)` label must add `.fixedSize(horizontal: true, vertical: false)` immediately after it** — this is now the required pairing, not just `.tracking(1.6)` alone.
- **Hiding a feature/module is developer-only and must never delete its code, model, or data — use the `DisableableFeature` system.** Disabling is controlled **purely in code**: `DisableableFeature.disabled` (a `Set` in `DisableableFeature.swift`) is the single source of truth, read via the pure computed `feature.isEnabled` (`!Self.disabled.contains(self)`). There is intentionally **no per-user persistence and no in-app toggle screen** — an earlier user-facing "Manage Features"/`DisabledFeaturesView` + `AppSettings.disabledFeatures` field were both removed once the user clarified the list is for developers, not end users. The running list is mirrored in `docs/DISABLED_FEATURES.md` — **keep that file and the `disabled` set in sync in the same commit**. Each case has a `category`: `.premium` rows are filtered out of `SettingsView`'s "Premium Features" grid via `visibleFeatures` + `destinationView(for:)` switch (whole card hidden if none remain); `.topLevelSection` cases (e.g. `.taxManagement`) wrap their own standalone `sectionCard` in `if DisableableFeature.X.isEnabled`; `.nested` cases live inside another card/screen (e.g. `.auditLog` inside `SecurityPrivacyView`'s `auditLogCard`, `.googleDriveBackup` as one row of the Data & Privacy card) and that call site checks `DisableableFeature.X.isEnabled` inline. Hiding UI is *usually* independent of the underlying mechanism continuing to run — e.g. hiding Audit Log's UI does not stop `AuditLogService.log(...)` calls elsewhere from still writing `AuditLogEntry` rows — **except when the feature actively transmits user data somewhere, in which case gate the background code path too, not just the entry point**: `.googleDriveBackup` additionally gates all 4 `GoogleDriveBackupService.shared.syncIfDue`/`.startAutoSync` call sites in `RootView.swift` behind `isGoogleDriveBackupEnabled` (= `.googleDriveBackup.isEnabled`), since leaving those running after "disabling" the feature would keep silently uploading the user's data to Google Drive in the background. When adding a new case, ask whether the underlying feature is passive (state/logging, safe to leave running) or active (network calls moving data externally, must be gated too). **Any future "disable/hide feature X" request should add a case to `DisableableFeature` (right `category`), add it to the `disabled` set, and record it in `docs/DISABLED_FEATURES.md` — nothing else** — never comment out or delete the feature's `NavigationLink`/`sectionCard`/view/model, and never rip out the code path that produces its underlying data. **Exception**: if the user explicitly says "remove"/"delete" rather than "disable"/"hide", confirm which they mean before acting (this codebase has treated the two very differently: every "disable X" got the non-destructive `DisableableFeature` treatment, but "remove Hidden Mode / Decoy PIN" — after an explicit confirmation prompt — got a full deletion: `HiddenModeView`, `PINSetupSheet.isDecoy`, `AppSettings.decoyPINHash`/`.hiddenModeEnabled`, `AppState.isHiddenMode`, and `AuditEventType.hiddenModeActivated` no longer exist in the codebase at all, not just hidden).
- **A `Budget.category` match is not enough to identify *which* budget a transaction belongs to once two budgets share a category** (e.g. "Netflix Budget" + "Spotify Budget", both `.subscriptions`). `BudgetView.swift` already had a keyword-disambiguation system for this (`Budget.merchantFilter`, or an auto-derived keyword from `Budget.name`) but it only ran Budget→spending, privately, inside that one file — `AddTransactionView.fireBudgetAlertIfNeeded` used a naive `budgets.first(where: { $0.category == category })` (ignoring `isActive` too) and summed the *entire* category's spend against whichever budget won that ordering, so multi-budget categories could alert against the wrong budget with the wrong total. Fixed by moving the matching logic into `BudgetService` (`autoKeyword`/`effectiveKeyword`/new `matchingBudget(...)`/`spending(...)`) as the single source of truth — `BudgetView.swift` now delegates to it, and `AddTransactionView` uses it both for the alert and for a live "Budget" indicator row. **Any new code that needs to know which budget a transaction affects must go through `BudgetService.matchingBudget(...)`, never a raw category `.first(where:)`.**

- **A new ingestion channel doesn't need a new `@Model` if an existing pending/rule pair already has the right shape — tag it instead, and gate any code that builds a channel-specific whitelist from the shared model.** SMS import (`SMSIngestService`) reuses `PendingEmailTransaction`/`BankEmailRule` unchanged, distinguishing itself only via an `"sms:<bank slug>"` sentinel in `senderAddress`/`senderEmail` — no schema bump. The one thing this pattern requires: any code that scans the shared model to build a channel-specific list (here, `EmailSyncService.sync`'s email-fetch sender whitelist) must explicitly exclude rows tagged for the other channel, or they leak in as silently-harmless-but-wrong entries (a `"sms:…"` string sent as a Gmail/IMAP search term). This is the same trick already used for `AppSettings.cloudSyncEnabled` (repurposed for local-backup auto-backup, not iCloud sync) — prefer it over a schema bump whenever a genuinely equivalent shape already exists.
- **One review-queue row per real transaction, not one per channel.** Email, SMS and Apple Pay all fire for the same card payment, so every filing path routes through `ImportDeduper.file(...)` **instead of `context.insert(...)`**: it finds a queued match (same weighted amount/time/merchant scoring as the duplicate flag, 0.6 threshold), keeps whichever reading scores higher on `ImportChannel.trustWeight` + confidence + field completeness, and absorbs the loser's complementary fields into the winner first. Any new ingestion channel must do the same — inserting directly reintroduces the duplicate rows this exists to prevent. Merging is confined to `.pending` items; a match against an approved ledger transaction still just gets flagged.
- **No automated import path (email sync, SMS) ever posts a transaction to the ledger without an explicit user tap.** Both `EmailSyncService.processEmail` and `SMSIngestService`'s filing step used to auto-approve above a confidence bar (`BankEmailRule.autoApprove`/`.confidenceThreshold`, plus a hardcoded default for SMS) — removed per explicit user request ("every automation import has to have a waiting list for user approval"). `approveToLedger(item:context:autoApproved:)` is now only ever called from `EmailReviewQueueView`'s swipe-to-approve / edit-sheet "Approve" (a human tap), never from either ingestion pipeline. The "Approve N high-confidence" bulk button in `EmailReviewQueueView` still exists and is fine — it's still an explicit tap, just on several items at once. `BankEmailRule.autoApprove`/`.confidenceThreshold` and `PendingEmailTransaction.wasAutoApproved` are kept on the model (no schema bump) but are dead going forward; their setup-wizard UI (`BankSetupWizardView`'s old Step 3, `SMSBankRuleSheet`'s old automation section) was removed rather than left showing a control with no effect. **Any future automated import channel must default to landing in the review queue — never wire a confidence threshold straight to `approveToLedger`.**
- **This project has NO App Group entitlement, and `UserDefaults(suiteName:)` does NOT tell you so — it fails silently and undetectably.** `FinTrack/FinTrack.entitlements` is present but **inert** (`CODE_SIGN_ENTITLEMENTS` appears zero times in `project.pbxproj`), deliberately: App Groups requires a **paid** Apple Developer Program membership, and wiring it under free personal-team signing fails code signing outright. The trap: `UserDefaults(suiteName: "group.com.fintrack.shared")` returns a **perfectly valid, non-nil object** without the entitlement — the initializer only returns nil for a nil name, the app's own bundle id, or `NSGlobalDomain`. Its writes are accepted and never persisted (the container directory doesn't exist), surviving only in that process's in-memory cache. So `guard let defaults = UserDefaults(suiteName:)` never guards, and `UserDefaults(suiteName:) ?? .standard` **never falls back** — both are no-ops dressed as working code. An AppIntent writes, reports success, the background-launched process exits, and the drain finds an empty queue. **Genuinely need the entitlement, still non-functional on device:** widget/Watch snapshots and the Siri/Watch `pending_transactions` queue, which really do cross process boundaries. **Do not need it:** the SMS and Apple Pay queues — their intents run *in the app's own process* (single application target, no App Intents Extension) — so `WidgetDataService.smsDefaults` is plain `.standard`. **Rule for new cross-surface state: if only the app process touches it, use `.standard` outright — never the group suite with a fallback, because the fallback is unreachable. And have the writer read the value back and return whether it landed, rather than assuming a `set` worked.** To enable App Groups properly (paid account): Xcode → target → Signing & Capabilities → + Capability → App Groups → `group.com.fintrack.shared`; Xcode wires `CODE_SIGN_ENTITLEMENTS` itself.
- **An AppIntent that takes real input must NOT be registered in `AppShortcutsProvider`.** App Shortcuts are fixed, zero-configuration entry points: they expose no parameter fields in the Shortcuts editor, can't be bound to variables, and tapping one does nothing. `LogTransactionFromText` was initially listed in `FinTrackShortcuts.appShortcuts`, so the Shortcuts *automation* picker offered that unconfigurable tile under "My Shortcuts" — its required `raw` (Message) parameter had no way to receive the incoming SMS, and iOS prompted "enter a message" on every single run (the user could only dismiss it blank or type the text by hand). Every `AppIntent` already appears in the Shortcuts editor as a normal configurable action **without** being in `appShortcuts`; that list is only for zero-input, voice-invocable commands (`LogExpenseIntent`, `GetBalanceIntent`, …). Corollary for the automation itself: in the "Do" step choose **Create New Shortcut** and add the action inside the editor — picking a ready-made shortcut/App Shortcut tile there gives you the unbindable form.
- **AppIntents cannot reliably touch SwiftData in-process.** Every intent in `FinTrackIntents.swift` (`LogExpenseIntent`/`LogIncomeIntent`/`LogTransactionFromText`) only ever writes a lightweight Codable payload into the shared App Group `UserDefaults` (`WidgetDataService.enqueuePendingTransaction`/`enqueuePendingSMS`) and returns immediately (`openAppWhenRun = false`); the actual `ModelContext` work happens later, on the main app process, when `RootView` drains that queue on `.onAppear`/`scenePhase == .active`. Follow this pattern for any new AppIntent that needs to create/modify persisted data — never construct a `ModelContainer`/`ModelContext` inside `perform()`.

## 9. STATE

Active branch: `claude/finance-app-features-f1fo3p`. No release/version tags found in this pass.

**Recently completed** (most recent commits, newest first):
- **Cross-channel merge: one queue row per transaction, holding the best data (user request).** Email/SMS/Apple Pay can all report the same payment; previously each arrived as its own row flagged "possible duplicate", so the user rejected two rows per purchase. New `ImportDeduper` now sits in front of every insert (`ImportFiler` for SMS/Apple Pay, `EmailSyncService.processEmail` for email): it matches against queued items, scores each reading (channel trust — Apple Pay structured > email verbose > SMS terse — plus parse confidence plus field completeness, minus a suspicious-parse penalty), keeps the winner, and **absorbs the loser's complementary fields into it** (reference number, balance, card last4, real merchant over a placeholder, account match, tags), so the surviving row is better than any single channel's version. `ImportChannel` gained `.email` + `of(_:)` detection + `trustWeight`. Email no longer double-notifies when a second channel reports a purchase it already queued. Merging is limited to `.pending` rows — a ledger match keeps the flag, since silently dropping there could hide a genuine repeat purchase.
- **New channel: Apple Pay ingestion via Shortcuts' built-in "Transaction" automation** (user's idea). Wallet hands Shortcuts *structured* fields rather than text, so this path has no parser at all — `LogApplePayTransaction` takes typed `amount`/`merchant`/`currency`/`date`/`walletCategory`/`card`/`isRefund` parameters and `ApplePayIngestService` builds a `ParsedBankEmail` straight from them (confidence 0.95), with Wallet's category mapped onto `TransactionCategory` via a new `categoryOverride` that beats merchant-based prediction. Because both channels now file identically, the filing step was extracted out of `SMSIngestService` into a shared `ImportFiler` + `ImportChannel` (per-channel tag/messageId/subject/lastImport differences only) so the two can't drift. New `ApplePayImportView` (Settings → Import & Integration → Apple Pay Import) mirrors the SMS setup walkthrough and diagnostics. Uses the same entitlement-free in-process queue as SMS. **Unverified on device** — the trigger's exact variable set, and whether it fires for non-Apple-Card Wallet cards in the UAE, still need real-world confirmation.
- **Root-caused "SMS automation runs but nothing appears": the queue write silently went nowhere, and the first fix for it was wrong.** The automation fired, `LogTransactionFromText.perform()` ran, returned its cheerful "Got it" dialog, stored nothing, and `RootView.drainPendingSMSTexts()` found an empty queue. The cause is the missing App Group entitlement (see §8) — but the initial fix was built on a false premise, that `UserDefaults(suiteName:)` returns **nil** without it. It does not: it returns a valid object whose writes are never persisted, so the `?? .standard` fallback that fix introduced was **dead code** and the bug survived untouched. **Actually fixed** by making `WidgetDataService.smsDefaults` plain `.standard` — correct because the SMS/Apple Pay intents run in the app's own process (single application target, no App Intents Extension), and `.standard` is the app's own container, which always persists. `migrateLegacyQueue(forKey:)` moves anything an earlier build stranded in the group suite. Both `enqueuePendingSMS`/`enqueuePendingApplePay` now **read the value back after writing** and return whether it landed, so the intent can't claim a success the store didn't give it. Diagnostics closed the remaining ambiguity: the intent logs each message as *"Delivered by Shortcuts — waiting for FinTrack to process it"* the moment it queues (`SMSIngestService.recordQueued`), and `ingest` **updates that entry in place** via its `queueId` rather than logging a second one — so "Recent Messages" now distinguishes *Shortcuts never reached the app* (no entry) from *it arrived but wasn't drained* (entry stuck on waiting) from *it arrived and couldn't be parsed* (entry with the parse outcome), which previously all looked identical. Still genuinely broken until someone buys a paid account: widgets, the Watch app, and Siri/Watch quick-add — all of which really do cross process boundaries (see §8).
- **Fixed SMS import's "enter a message" prompt — root cause was registering `LogTransactionFromText` as an App Shortcut.** Reported on-device as: the automation fires, but Shortcuts stops and asks the user to type a message, and tapping the "Log Transaction" action in the automation does nothing. Cause: the intent was in `FinTrackShortcuts.appShortcuts`, so the automation's "Do" picker surfaced the fixed App Shortcut tile (no parameter fields, nothing bindable, not tappable) instead of the configurable editor action, leaving the required `raw` parameter unfilled → iOS prompts for it at runtime. Removed it from `appShortcuts` (the intent still appears as a normal action — every `AppIntent` does) and rewrote `SMSImportView`'s walkthrough to say **Create New Shortcut** in the "Do" step rather than picking the ready-made tile. See the new §8 bullet. Verified along the way that the app-side pipeline is correct: pasting the SMS text by hand into the prompt parses and files it into the review queue exactly as intended.
- **Cross-channel (email + SMS) duplicate detection, per user request.** `ImportLearningService.duplicateCheck`'s weighted similarity scoring previously ran only against `existingTransactions` (the real ledger) — a same real-world transaction reported by both email and SMS landed as two separate pending items that rarely share an exact fingerprint (SMS often omits the reference number email carries, or extracts a shorter merchant string), so the two could sit side-by-side in the queue unflagged. Added a second scoring pass against `pendingItems` (new `similarityScore(...against: PendingEmailTransaction)` overload, sharing a private `score(...)` helper with the existing `Transaction` overload) so a cross-channel match is now flagged just like a ledger match, with a reason naming which channel it collided with. `EmailReviewQueueView` no longer lets a flagged item post straight to the ledger from either the swipe action or the edit sheet — both route through a new "Approve Anyway" confirmation dialog, and the edit sheet now shows the duplicate warning inline (it previously showed nowhere in that sheet, only as a small badge on the queue row). Also fixed: the edit sheet's "SOURCE EMAIL" section always said "email" even for SMS-derived items.
- **Removed auto-approve from every automated import path (email + SMS), per explicit user request.** `EmailSyncService.processEmail` and `SMSIngestService`'s filing step no longer call `approveToLedger(..., autoApproved: true)` under any condition — see the new §8 bullet. Their setup UIs (`BankSetupWizardView` Step 3, `SMSBankRuleSheet`'s automation section, both toggle+slider) were removed along with the now-misleading "Auto ≥X%" badges in `EmailImportView`/`SMSImportView`'s bank rows. `BankSMSParser.parse` was simplified from returning an `Outcome{results, source}` wrapper to a plain `[ParsedBankEmail]`, since `source` (template vs on-device model) had no remaining consumer once auto-approve eligibility stopped depending on it.
- **New feature: SMS → transaction, via a Shortcuts automation (no iOS SMS read API exists for third-party apps).** New `LogTransactionFromText` AppIntent (silent, `openAppWhenRun = false`) is fed by a per-bank "When I receive a message" Personal Automation; it queues the raw text (`WidgetDataService.enqueuePendingSMS`, App Group key `"pending_sms_texts"`) for `RootView.drainPendingSMSTexts()` to parse on next foreground. `BankSMSParser` tries a deterministic on-device template match first (`BankSMSTemplateStore` — a bundled + optionally-remote-refreshable UAE bank sender-ID list, reusing `BankEmailParser`'s existing extraction grammar rather than duplicating it), falling back only when unmatched to Apple's on-device `FoundationModels` model (new `FoundationModelsSMSExtractor`/`SMSExtractionTypes.swift` `@Generable` schema, instructed by the verbatim extraction system prompt in `TransactionExtractionPrompt.swift`); every model-returned evidence span is checked against the message with the new `TextNormalizer.isGrounded` before being trusted, and a model-sourced result is capped at a lower confidence than the deterministic path can reach. `SMSIngestService` files results into the **existing** `PendingEmailTransaction` review queue, tagging SMS-origin rows via an `"sms:<bank slug>"` sentinel on `senderAddress`/`BankEmailRule.senderEmail` — see §8 — so **no new `@Model` and no schema bump** were needed; every item waits for the user's own approval (see the auto-approve-removal bullet above). New `SMSImportView`/`SMSBankRuleSheet` (Settings → Import & Integration → SMS Import) walks the user through the Shortcuts setup (`shortcuts://create-shortcut` deep link) and per-bank rule config (bank + SMS sender ID + linked account), with a first-SMS-within-24h status card. `EmailReviewQueueView` shows a small message-icon badge on SMS-sourced rows; `EmailSyncService.sync`'s email-fetch sender whitelist explicitly excludes `"sms:"`-tagged rules. **Caveat**: the exact `FoundationModels`/`LanguageModelSession` API surface in `FoundationModelsSMSExtractor.swift` was written from training knowledge and could not be compiler-verified (no Xcode/CLI build in this environment) — if it doesn't compile as-is, the fix is contained to that one file; the template-match path (which handles the common case) doesn't depend on it compiling correctly. Also unbuilt: hosting real per-bank `.shortcut` files and standing up a remote-templates endpoint are both outside what this session could produce (`BankSMSTemplateStore.refreshFromRemote(url:)` is the wired-but-inert client hook for the latter).
- **Backup screen trimmed to one backup, no explanatory copy.** `maxKeptBackups` 10 → **1**, so each backup replaces the previous one; `pruneOldBackups()` is now internal and also runs from `LocalBackupView.reload()` so devices carrying up to 10 files from the old policy collapse to one on next open. The four-paragraph info card was deleted, the list header is "LATEST BACKUP", and the now-meaningless "Backups" count tile was dropped (Size + Reinstall-safe remain).
- **Automatic backup is now change-driven instead of launch-driven.** `LocalBackupService.startObservingChanges(container:)` (called once from `RootView.task`) observes `ModelContext.didSave` and debounces 4 s, so a backup runs shortly after the user's *last* edit in a burst rather than on every launch/resume. `scheduleAutomaticBackupIfNeeded` and the 24 h interval were deleted along with `RootView`'s three launch/background/active call sites. Both restore paths run inside `withAutoBackupSuppressed` so an import's own `save()` can't re-trigger a backup, and a queued backup waits out an in-flight one instead of being dropped. The switch is still `AppSettings.cloudSyncEnabled`, relabelled "Back Up After Every Change" in `LocalBackupView`.
- **Backups made user-inaccessible and reinstall-proof.** Files moved from `Documents/Backups` (Files-app visible) to **Application Support/Backups**, and `UIFileSharingEnabled`/`LSSupportsOpeningDocumentsInPlace` were removed from `FinTrack-Info.plist` — backups are now invisible and un-editable; the UI dropped its delete/share actions (Restore only). Because iOS erases the entire app container on delete, file-based backups **cannot** survive an uninstall, so a second layer was added: a slimmed (no images/documents), compressed, encrypted **Keychain device snapshot**, which survives app deletion and is reclaimed by the same bundle id. `RootView` gates launch on `restoreFromDeviceSnapshotIfNeeded` and auto-restores into an empty store, then skips onboarding. Snapshot is capped at 2 MB; a legacy-`Documents/Backups` migration runs on first access. Still true: nothing survives erasing the device or moving to a new one — Drive/Email backup remain the off-device options.
- **iCloud backup replaced with on-device backup** (labeled simply **"Backup"** in the UI). New `LocalBackupService` + `LocalBackupView` write timestamped encrypted `.fintrack` files to `Documents/Backups`, listed in-app with restore / share-to-Files / delete, auto-pruned to the 10 newest, plus an automatic daily backup. `FinTrack-Info.plist` gained `UIFileSharingEnabled` + `LSSupportsOpeningDocumentsInPlace` so the folder shows up in Files under On My iPhone → FinTrack → Backups. Deleted `iCloudBackupService.swift` + `iCloudSyncView.swift` (the ubiquity container needed a paid developer account); Settings/Import rows, `RootView`'s 3 auto-backup call sites, and the Privacy Policy / Terms wording all updated. The automatic-backup switch reuses `AppSettings.cloudSyncEnabled` (meaning changed, no schema bump); `AppSettings.backupWifiOnly` is now orphaned but kept — removing a non-optional property would wipe the store.
- **Floating tab bar upgraded to real iOS 26 Liquid Glass.** `CustomTabBar` (`RootView.swift`) now uses `.glassEffect(.regular, in: .capsule)` instead of a flat `.ultraThinMaterial` rounded-rect background; the selected-tab pill is a neutral `FTColor.textPrimary.opacity(0.10)` capsule (adapts light/dark) kept in place with the existing `matchedGeometryEffect`. iPhone only (`MainTabView`); iPad's `NavigationSplitView` sidebar is unaffected.
- **The Cloudflare email-sync backend feature was added then reverted** (commit `b00c6e6` reverted by `63c5e4e`) per user request. The `fintrack-email-sync` D1 database created for it still exists on the user's Cloudflare account (empty). No trace remains in the app code.
- **Backups now cover the whole app** (per request). `DataTransferService` previously serialized only ~24 core models; extended it (new `DataTransferService+ExtraBackup.swift`) to every `@Model` **except** the transient `PendingEmailTransaction` review queue — Tax, Business, Family, Premium planning, budget envelopes/templates, gift cards, loyalty, custom categories (+parent links), categorization rules, email-import config (mailboxes/bank rules), remittance, insurance, import history, audit log, net-worth milestones, and document attachments (+transaction links). All backup methods (file export/import, iCloud, Drive, Email) share this engine, so all now capture everything. Infra: `BackupDTO`/`BackupIdentifiable` protocols + generic `restoreExtras()`; DTOs mirror stored props and rebuild via default-init + assignment. `FinTrackBackup` fields are optional (old backups still decode); backup version 6→7. NOT a schema change — no `@Model` added, no schema-version bump.
- **Restored just the "Import Backup" row** in Settings → Data & Privacy (per request), after both Export+Import were removed earlier. Re-added the `.fileImporter` + merge/replace confirmation + result alert + `runImport`/`runImportAsync` + the `showingImporter`/`pendingImportURL`/`showingImportMode`/`showingResult`/`resultMessage` state + the `UniformTypeIdentifiers` import. **Export Backup and Export as CSV stay removed.**
- **Foreign-currency transactions now lock their base value at the transaction-time rate (app-wide).** Root cause: many `Transaction(...)` creation sites omitted `amountInBaseCurrency` (so it defaulted to the raw foreign `amount`) or a couple of display paths re-converted the stored base value with today's live rate — so a foreign payment's base equivalent was wrong and drifted. Fixes: (1) added `CurrencyService.amountInBase(_:from:)` + `baseCurrencyCode` and passed `amountInBaseCurrency:` at every foreign-capable creation site — `DebtManagementView` (BNPL/lent/borrowed repayments), `AddMoneyLentView`/`AddMoneyBorrowedView`, `FreelanceView`, `RentalView`, `AddDividendView`, `InvestmentPortfolioView` (sale), `BillDetailView`, `AccountDetailView`, and the CSV/OFX/PDF importers; (2) `IncomeManagementView`'s last/prev-month income now sums stored `amountInBaseCurrency` directly instead of live-re-converting it (was double-counting foreign income); (3) `TransactionRowView` + `TransactionDetailView` now show the locked base equivalent ("≈ base", and the detail shows `1 CUR = rate BASE · rate on <date>`) for foreign-currency transactions. Rule (see MAP_Models): store base at creation via `amountInBase`, then read `amountInBaseCurrency` directly — never re-convert live.
- **Merchant→category map lookup: hidden from UI, always-on, and optimized.** Removed the "MERCHANT CATEGORIES" section (the "Category from Maps" toggle + "Google Maps Key" field) from `EmailImportView` — it's now a silent feature. `MerchantCategoryService.isEnabled` is a hardcoded `true` (was a UserDefaults toggle). Accuracy work: country-biased lookups via `regionCode` (`Locale.current.region`, default AE) applied to both Google (`regionCode`) and Nominatim (`countrycodes`); Nominatim now pulls up to 10 results and walks them for the first that maps (was top-1 only); Google reads `primaryType` before the broader `types`; the place-type→category table roughly doubled (adds `hypermarket`→food, `mall`/`marketplace`/`mobile_phone`/`cosmetics`/… →shopping, `car`/`tyres`→transportation, etc.). Cache key bumped v2→v3 so every merchant is re-evaluated with the stronger logic.
- Removed the "Set Up Your First Bank" empty-state card from Email Import's MY BANKS section (the "+ Add Bank" header button remains the entry point).
- Removed the local file **"Export Backup" and "Import Backup" rows** from Settings → Data & Privacy, plus all their now-dead machinery (`exportBackup`/`runImport`/`runImportAsync`/`presentShareSheet`, the `.fileImporter` + import-mode dialog + result alert, the `showingImporter`/`pendingImportURL`/`showingResult`/`resultMessage` state, and the 11 export-only `@Query` fetches led by `exportAccounts`). The card is now iCloud/Drive/Email backup links + Clear All Data. `DataTransferService` export/import still exist as APIs (used by the cloud backup services) but are no longer reachable from Settings.
- Removed the **"Export as CSV" row** from Settings → Data & Privacy (and its `exportCSV()` helper + now-unused `transactions` `@Query`). CSV export still exists in the **Reports** tab (`ReportsView` export menu → `ReportExportService`); only the Settings entry point is gone.
- **Bottom-nav tab taps now pop that tab to its main page.** Tapping a tab (including re-tapping the current one while deep in a pushed screen, e.g. Accounts → Debt Management) lands on the tab's root. `CustomTabBar` bumps `AppState.popToRootTick` on every tab tap (before the `selectedTab != tab` guard, so re-taps count); each tab root watches it via `.onChange(of: appState.popToRootTick)` and clears its pushed-screen bindings (`AccountsView`/`BudgetView` `moduleRoute`, `BudgetView` `detailGoal`, `DashboardView` `dashRoute`, `TransactionsListView` `showingEmailReviewFromBanner`). Only item/isPresented-driven pushes reset this way — a direct `NavigationLink(destination:)` (e.g. Transactions' Email Review Queue) isn't tracked by a binding and won't pop.
- **Removed the backup-encryption UI** (per user request "don't show backup encryption to user"): deleted `BackupEncryptionSettingsView.swift` and both entry points to it — the "Backup Encryption" row in `SettingsView`'s "Data & Privacy" card and the one in `iCloudSyncView`'s settings card. Backup encryption is unchanged and still **mandatory/always-on** (`BackupEncryptionService`); it's just no longer surfaced anywhere in the UI. (File deletion is safe — the Xcode project uses `PBXFileSystemSynchronizedRootGroup`, so files aren't listed individually in `project.pbxproj`.)
- **Fixed freeze when opening the pushed module screens** (Income/Portfolio/Savings Goals/Assets & Liabilities/Debt, plus Reports/Settings). Root cause: each presenter stacked several `.navigationDestination(isPresented:)` modifiers on one `NavigationStack` — the competing `isPresented` bindings all drive a single navigation slot, so SwiftUI re-resolved the stack every frame (main-thread spin → freeze) and often refused to push. Fix: one enum-driven `.navigationDestination(item:)` per presenter (`ModuleRoute` in `AccountsView`, `BudgetModuleRoute` in `BudgetView`, `DashboardRoute` in `DashboardView`); exactly one destination is active at a time. Rule: never stack multiple `isPresented` destinations on the same stack — use a single `item:`/path-based destination.
- Floating tab bar (`CustomTabBar`) now shrinks ~20% while scrolling down and animates back when scrolling up / near top (Instagram-style), per a reference video. Driven by `AppState.tabBarCollapsed`, set by a reusable `.collapsesTabBarOnScroll()` modifier (`onScrollGeometryChange`, iOS 18+) attached to the 4 tab roots' main scroll views; the bar reads it via `.scaleEffect(0.8, anchor: .bottom).animation(...)`. The modifier writes `tabBarCollapsed` via `DispatchQueue.main.async` (deferred off the scroll/layout pass) so mutating state the sibling `CustomTabBar` reads doesn't re-invalidate the shared hosting view's layout from inside its own layout pass — that was triggering SwiftUI's "observation tracking feedback loop" warning; the hysteresis thresholds keep the value from oscillating. Pushed screens (Settings etc.) don't yet report scroll, so they keep whatever state the last tab left (tab-change + near-top both reset to full size).
- Income Management, Investment Portfolio, Savings Goals, Assets & Liabilities, and Debt Management now open as **pushed** screens instead of sheets — same as Settings. Presenters (`AccountsView` for all 5; `BudgetView` for Income + Debt) use a single enum-driven `.navigationDestination(item:)` (see freeze fix above); each of the 5 views dropped its own top-level `NavigationStack` + "Done" toolbar (back button dismisses) and bumped bottom padding to 120 to clear the floating tab bar. Their internal detail/add **sheets** and any nested `NavigationStack` inside a `.sheet` stay as-is.
- Settings now opens as a pushed screen instead of a modal sheet: `DashboardView` pushes it via a single enum-driven `.navigationDestination(item: $dashRoute)` (shared with Reports), and `SettingsView` dropped its own `NavigationStack` wrapper + "Done" toolbar (back button dismisses) so it doesn't nest stacks.
- Backup encryption made mandatory/always-on with a random device-only key (per explicit user request): `BackupEncryptionService` no longer uses a user passphrase or an opt-in toggle — it auto-generates a random 256-bit key stored only in this device's Keychain and encrypts every backup (manual/iCloud/Drive/email). Removed `storedPassphrase`/`isEnabled` + the passphrase-management UI; the read-only status screen (`BackupEncryptionSettingsView`) was later deleted too — see the top entry. Backups are readable only by this FinTrack install (no other app, no other device/reinstall).
- "Hide Balances" feature removed entirely (per explicit user request): deleted the `AppState.hideBalances` field, its Settings toggle row, and the `••••`/`••••••` conditional rendering + accessibility branches at both use sites (`AccountsView` net-worth hero, `DashboardView` account cards now always show real balances). `Account.isHidden` (hide individual accounts from dashboard/net worth) and `AmountDisplayView(isHidden:)` are separate features, left intact.
- `DisableableFeature` converted to developer-only control (per user clarification: the disabled list is for developers, not an end-user selection UI): removed the user-facing `DisabledFeaturesView` + "Manage Features" Settings row and the `AppSettings.disabledFeatures` per-user field; the `disabled` `Set` in code is now the sole source of truth (read via `feature.isEnabled`), mirrored in the new `docs/DISABLED_FEATURES.md`.
- Google Drive Backup disabled by default: new `.nested`-category `DisableableFeature` case (`.googleDriveBackup`), hides the Settings row (one sibling row inside the shared "Data & Privacy" card) and — unlike other `.nested` cases — also gates `RootView`'s 4 `GoogleDriveBackupService` background-sync call sites, so anyone previously connected/enabled stops actively syncing to Google Drive, not just losing the settings entry point.
- Local data encryption forced always-on: removed `AppSettings.encryptionEnabled` and the "End-to-End Encryption" toggle (now a static "Always on" row in `SecurityPrivacyView`); scrubbed "AES-256" from all user-facing text app-wide (3 spots) per explicit request to never mention encryption type/algorithm to the user.
- Hidden Mode / Decoy PIN fully removed (not just hidden — explicit user request to delete, confirmed via AskUserQuestion first): `HiddenModeView` struct, `PINSetupSheet.isDecoy` + its decoy branches, `AppSettings.decoyPINHash`/`.hiddenModeEnabled` fields (+ their `AppSettingsDTO` backup/restore plumbing in `DataTransferService.swift`), `AppState.isHiddenMode` (was already dead/unused), and `AuditEventType.hiddenModeActivated` are all gone from the codebase. No schema-version bump — SwiftData tolerates removed columns without one (only additions of non-optional properties require a bump).
- Audit Log UI hidden: new `.nested`-category `DisableableFeature` case (`.auditLog`); `SecurityPrivacyView.auditLogCard` (the "Enable Audit Logging" toggle + "View Audit Log" screen) is conditionally rendered — background audit logging (`AuditLogService.log`) is unaffected and keeps writing `AuditLogEntry` rows regardless of UI visibility.
- The `DisableableFeature` set grew over several commits (Collaborative Planner, Insurance Optimizer, Remittance Tracker as `.premium`; Tax Management + Business & Freelancer as `.topLevelSection`, which introduced the `category` distinction). All now live in the developer-only `disabled` set + `docs/DISABLED_FEATURES.md` (see the top bullet for the later removal of the user-facing toggle). Every one hides UI only; code/data stay intact.
- Bills tab in Debt Management (`SubscriptionsTabContent` embedded), Bill payment "Pay From" account picker with linked `Transaction`/balance deduction (`RecordPaymentSheet`), and `AddTransactionView`'s manual "Paying Bill" picker linking a paying transaction to its `Bill` (auto-linking by title/merchant match was removed).
- Budget AI-recognition: `AddTransactionView` shows a live read-only "Budget" row and `fireBudgetAlertIfNeeded` now routes through `BudgetService.matchingBudget`/`.spending` instead of a naive category `.first(where:)`.
- Accounts & Assets redesign (per a claude.ai/design mockup, imported via the `DesignSync` MCP tool): `AccountsView.swift` rebuilt with a net-worth hero, a 5-card module grid (Income/Investments/Goals/Assets & Liabilities/Debt), and a segmented Accounts/Investments/Crypto/Assets tab list. Income Management, Investment Portfolio, Savings Goals, Assets & Liabilities, Net Worth, and Debt Management are no longer on `DashboardView` — their sheets are now opened from the Accounts page's module cards instead, reusing the same underlying feature views unchanged. `DashboardWidget`'s now-dead `.income`/`.investments`/`.goals`/`.debt` cases were removed from `DashboardCustomizerView` accordingly.
- BNPL parity with Loan: edit, currency picker, delete-with-cleanup, and a full Record Payment flow (new `linkedBNPL` relationship + `bnplRepayment` category on `Transaction`).
- Cross-currency conversion fixes across Budget/SavingsGoal totals, rollover processing, forecasts, and alerts (`BudgetService`, `BudgetView`, `ReportsView`, `AddSavingsGoalView`, `SmartCashAllocationView` partially).
- Loan detail sheet + discoverable Record Payment button (previously buried in a long-press menu); swipe-to-delete removed in favor of the same long-press context-menu pattern used by Lent/Borrowed.
- Full 24-currency picker rolled out to every remaining truncated currency list app-wide.
- Email/SMTP backup (no-OAuth alternative to Google Drive/iCloud) with hourly auto-backup via a real foreground polling loop.
- AES-256-GCM backup encryption (passphrase Keychain-only, `BackupEncryptionService`).
- `AmountTextField` (thousands-separator input) rolled out to 35+ files app-wide.
- Google Drive backup upgraded to two-way "gossip" sync.
- Various Debt Management edit/delete fixes (orphaned account-deduction bugs across Lent/Borrowed/Loan).
- Section-header clipping fix app-wide (`.tracking(1.6)` needs `.fixedSize(horizontal: true, vertical: false)`, 229 occurrences/67 files).
- `AddMoneyBorrowedView` account picker + linked-`Transaction` logic (was missing entirely; `AddMoneyLentView` had it, `AddMoneyBorrowedView` didn't) + the per-transaction-type delete-reversal fix it exposed.
- `AddEnvelopeView` currency picker + the cross-currency correctness fixes it required (`EnvelopeRow`, `envelopeOverviewCard`, `EnvelopeDetailView`, envelope-to-envelope Transfer).

**Known TODOs / dormant issues** (flagged during this mapping pass, not yet fixed):
- `SmartCashAllocationView.totalCash` sums `Account.balance` across accounts without currency conversion.
- No automated test suite exists at all.
